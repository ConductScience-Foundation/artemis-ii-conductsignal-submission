# Optional dependencies for rebuilding public proxy inputs and running QA.
# The core command `python artemis\run_demo.py` uses only the Python standard library.
pandas>=2.0
openpyxl>=3.1
pytest>=8.0
